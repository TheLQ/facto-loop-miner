{
  "name": "facto-loop-miner",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p .",
    "buildrun": "tsc -p . && node --enable-source-maps dist/hello.js"
  },
  "dependencies": {
    "big-json": "^3.2.0",
    "jimp": "^0.22.10"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
